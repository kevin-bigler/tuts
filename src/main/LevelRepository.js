/**
 * Accesses levels from the database
 */
class LevelRepository {
    /**
     * returns a level from the database
     * @param id
     */
    get(id) {

    }

    /**
     * updates a level that already exists in the database
     * @param id
     * @param level
     */
    update(id, level) {
        // CRUD - Create, Read, Update, Delete
    }
}

module.exports = LevelRepository;